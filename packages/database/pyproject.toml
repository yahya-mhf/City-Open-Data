[tool.poetry]
name = "smart-city-database"
version = "0.1.0"
packages = [{ include = "smart_city_database" }]

[tool.poetry.dependencies]
python = "^3.12"
sqlalchemy = "^2.0"
asyncpg = "^0.29"
alembic = "^1.13"
smart-city-shared = {path = "../shared", develop = true}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
