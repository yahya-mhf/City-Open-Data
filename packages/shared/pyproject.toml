[tool.poetry]
name = "smart-city-shared"
version = "0.1.0"
packages = [{ include = "smart_city_shared" }]

[tool.poetry.dependencies]
python = "^3.12"
pydantic = "^2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
