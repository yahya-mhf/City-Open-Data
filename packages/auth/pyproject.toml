[tool.poetry]
name = "smart-city-auth"
version = "0.1.0"
packages = [{ include = "smart_city_auth" }]

[tool.poetry.dependencies]
python = "^3.12"
pyjwt = "^2.8"
argon2-cffi = "^23.1"
smart-city-shared = {path = "../shared", develop = true}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
